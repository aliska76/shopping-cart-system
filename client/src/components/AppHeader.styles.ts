import type { SxProps, Theme } from '@mui/material/styles';

export const toolbarSx: SxProps<Theme> = {
  display: 'flex',
  justifyContent: 'space-between',
};

export const titleBoxSx: SxProps<Theme> = {
  display: 'flex',
  alignItems: 'center',
  gap: 1,
};
