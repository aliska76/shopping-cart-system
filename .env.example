# Copy to .env (gitignored) and set a real value locally.
# .env is never committed — docker-compose reads it automatically.
MSSQL_SA_PASSWORD=Change_this_locally_123!
MSSQL_PORT=1433
CATALOG_DB_NAME=CatalogDb
ELASTICSEARCH_PORT=9200
